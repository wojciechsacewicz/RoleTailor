# Local RoleTailor workspace

This workspace is generated from the profile confirmed during onboarding. `profile/profile.json` and `docs/career-vault.md` are the factual source of truth; application runs may tailor wording, but must never invent facts.

The source repository contains only this neutral template. Real career data is written under the operating system's application-data directory and is never required in Git.
