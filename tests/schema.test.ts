import {describe,expect,it} from 'vitest';
import {parseResult} from '../src/lib/backend';

const valid={runId:'r1',company:'Acme',role:'Product Engineer',jobUrl:null,language:'en',fit:{stars:4,matchScore:78,inviteLikelihoodEstimate:52,confidence:'medium',recommendation:'apply-with-caveats',summary:'Strong product evidence, with one stack gap.',strengths:['Product ownership'],gaps:['No Kotlin evidence'],dealbreakers:[],differentiators:['AI-assisted delivery']},artifacts:{cvPdf:'dist/acme.pdf',cvSource:'variants/acme.md',applicationMessage:'Hello',coverLetter:'Longer note'},changeSummary:['Moved product work up'],cvReview:[{target:'skills',title:'Clarify the stack',comment:'Keep the Kotlin gap explicit.',priority:1}]};

describe('result schema',()=>{it('accepts a complete manifest',()=>expect(parseResult(valid).fit.stars).toBe(4));it('rejects inflated and malformed scores',()=>expect(()=>parseResult({...valid,fit:{...valid.fit,stars:6}})).toThrow());it('requires artifact paths and written output',()=>expect(()=>parseResult({...valid,artifacts:{cvPdf:'x'}})).toThrow());it('rejects annotations outside known CV sections',()=>expect(()=>parseResult({...valid,cvReview:[{...valid.cvReview[0],target:'random'}]})).toThrow());it('requires nullable jobUrl and rejects extra fields',()=>{expect(()=>parseResult({...valid,jobUrl:undefined})).toThrow();expect(()=>parseResult({...valid,unexpected:true})).toThrow()})});
