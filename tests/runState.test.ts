import{describe,expect,it}from'vitest';import{canTransition}from'../src/lib/runState';describe('run state transitions',()=>{it('allows the production pipeline',()=>{expect(canTransition('Analysing','Tailoring CV')).toBe(true);expect(canTransition('Building PDF','Completed')).toBe(true)});it('does not reopen a completed run implicitly',()=>expect(canTransition('Completed','Analysing')).toBe(false))});
