name: Canvas rebuild bootstrap

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bootstrap:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Payload upload in progress"
